Required participants: 2 + group manager

Once all participants (gm + p1 and p2) are alive, p1 sends a multicast.
In step 2 p1 send a multicast which only p2 receives, then p1 and p2 both
crash.

In step 5, p1 and  p2 revive as p3 and p4 and in step
6 the group manager (p0) sends a multicast.