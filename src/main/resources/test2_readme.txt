Required participants: 3+ group manager

Once all participants (gm + p1, p2 and p3) are alive, p1 sends a multicast.
In step 2 p1 sends a multicast and crashes.
When the new view is being installed, p2 receives the view change message
and crashes.

In step 4 only p2 revives as p4.